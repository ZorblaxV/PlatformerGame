	var titleScreen = document.getElementById("myBugbear");//obvs will change this in a bit

	function drawTitleScreen() {//this function draws the title screen
		ctx.drawImage(titleScreen, 0, 0, canvas.width, canvas.height);
	}